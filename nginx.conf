events {}

http {
  map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
  }

  upstream websocket_backend {
    # All your listener containers
    server listener1:5001 max_fails=1 fail_timeout=5s;
    server listener2:5002 max_fails=1 fail_timeout=5s;
    server listener3:5003 max_fails=1 fail_timeout=5s;

    keepalive 32;
  }

  server {
    listen 5000;

    # Allow WebSocket upgrade requests
    location / {
      proxy_pass http://websocket_backend;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_set_header Host $host;
      proxy_read_timeout 3600s;

      # For logging/debug
      access_log /var/log/nginx/access.log;
      error_log /var/log/nginx/error.log;
    }
  }
}